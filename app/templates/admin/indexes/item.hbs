{{page-title "Item"}}

<h1 class="text-2xl mb-4">{{capitalize @model.name}}</h1>

<div class="flex">
  <Tab @route="admin.indexes.item.data" @text="Data" @icon="database-outline" @model={{@model.name}} />
  <Tab @route="admin.indexes.item.settings" @text="Settings" @icon="cog-outline" @model={{@model.name}} />
</div>

{{outlet}}
