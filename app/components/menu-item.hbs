<LinkTo @route={{@route}} @model={{@model}} @activeClass="bg-secondary" class="flex items-center px-4 py-2 rounded-md text-light hover:bg-secondary">
  {{#if @icon}}
    <MdIcon @icon={{@icon}} />
  {{/if}}
  <span class="{{if @icon 'mx-4'}} font-medium">{{@name}}</span>
</LinkTo>
