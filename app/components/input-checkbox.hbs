<label class="flex items-center cursor-pointer" {{on "click" this.onChange}} ...attributes>
  {{#if @checked}}
    <Icons::CheckboxOutline @size={{22}}/>
  {{else}}
    <Icons::CheckboxBlankOutline @size={{22}}/>
  {{/if}}
  <span class="ml-1">{{@label}}</span>
</label>
