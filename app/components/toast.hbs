{{#in-element this.toaster.destinationElement insertBefore=null}}
  <div
    class='fixed border rounded shadow-sm'
    role='alert'
    style={{this.style}}
    {{did-insert this.didInsert}}
    ...attributes
  >
    <div class='p-4'>
      {{yield (hash invokeOnClose=this.invokeOnClose)}}
    </div>
    {{#if @autoClose}}
      <ProgressBar
        @duration={{this.autoCloseDuration}}
        @onFinish={{this.invokeOnClose}}
        class={{@closeIndicatorClass}}
      />
    {{/if}}
  </div>
{{/in-element}}
