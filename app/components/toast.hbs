{{#in-element this.toasts.destinationElement insertBefore=null}}
  <div
    class='flex items-center p-4 w-full max-w-xs border rounded shadow-sm'
    role='alert'
    {{did-insert this.registerInQueue}}
  >
    {{yield (hash triggerOnClose=this.triggerOnClose)}}
  </div>
{{/in-element}}
